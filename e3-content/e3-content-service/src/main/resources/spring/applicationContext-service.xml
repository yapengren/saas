<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <!-- 配置包扫描器 -->
    <context:component-scan base-package="com.yapengren.e3mall.content.service"/>

    <!-- 使用 dubbo 发布服务 -->
    <!-- 提供方应用信息，用于计算依赖关系 -->
    <dubbo:application name="e3-content"/>
    <dubbo:registry protocol="zookeeper" address="zkserver:2181"/>
    <!-- 用 dubbo 协议在 20881 端口暴露服务 -->
    <dubbo:protocol name="dubbo" port="20881"/>
    <!-- 声明需要暴露的服务接口 -->
    <dubbo:service interface="com.yapengren.e3mall.content.service.ContentCatService" ref="contentCatServiceImpl" timeout="1200000"/>
    <dubbo:service interface="com.yapengren.e3mall.content.service.ContentService" ref="contentServiceImpl" timeout="1200000"/>
</beans>